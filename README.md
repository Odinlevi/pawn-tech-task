# Technical task for Vibe Games.

## Description: 
A server side app made with open.md that controls the behaviour of greenhouses that are managed by players.

## Initialization steps:
1. Initialize SQLite scheme
`sqlite3 server_realtime_data.db < sql/init_db.sql`
